import React from "react";
import { View, Text, StyleSheet } from "react-native";
// ---- Components ---- //

const Qr = () => {
  return (
    <View style={styles.container}>
      {/* Здесь будет Qr для каждого пользователя он будет генерироватся только один раз дл каждого адреса доставки при их добовление  
      
      нужно сделать если у пользователя есть только один адрес доставки нужно 
      */}
    </View>
  );
};

const styles = StyleSheet.create({});

export default Qr;
